import 'package:flutter/material.dart';

class AppColors {
  // Dark Theme (Aviation/Tactical)
  static const Color darkBackground = Color(0xFF0F172A); // Slate 900
  static const Color darkGlass = Color(0x661E293B);      // Semi-transparent
  static const Color accentNeon = Color(0xFF00F2FF);     // Cyan Telemetry

  // Light Theme (Clean/Outdoor)
  static const Color lightBackground = Color(0xFFF8FAFC);
  static const Color lightGlass = Color(0x99FFFFFF);     // Frosty white
  static const Color accentBlue = Color(0xFF2563EB);
}